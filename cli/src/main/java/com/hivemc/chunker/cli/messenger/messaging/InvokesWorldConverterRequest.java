package com.hivemc.chunker.cli.messenger.messaging;

/**
 * Interface to indicate that a BasicMessage starts a world conversion request when sent (and needs to be cancelled).
 */
public interface InvokesWorldConverterRequest {
}
