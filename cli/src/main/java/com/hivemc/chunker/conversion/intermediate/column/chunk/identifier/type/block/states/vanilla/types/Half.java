package com.hivemc.chunker.conversion.intermediate.column.chunk.identifier.type.block.states.vanilla.types;

import com.hivemc.chunker.conversion.intermediate.column.chunk.identifier.type.block.states.BlockStateValue;

/**
 * The half of the block that this block displays.
 * (Used for blocks with two parts)
 */
public enum Half implements BlockStateValue {
    TOP,
    BOTTOM
}
